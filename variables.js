// Declare a variable with the name 'age' and assign it your age.
let age = 30;
console.log(age);  // Should output: 30

// Declare a constant variable with the name 'PI' and assign it the value of pi (3.14159).
const PI = 3.14159;
console.log(PI);  // Should output: 3.14159

// Declare a variable with the name 'name' and assign it your first name as a string.
let name = "John";
console.log(name);  // Should output: John

// Declare a variable with the name 'price' and assign it the price of an item (e.g., 19.99).
let price = 19.99;
console.log(price);  // Should output: 19.99

// Declare a variable with the name 'isLogged' and assign it the boolean value true.
let isLogged = true;
console.log(isLogged);  // Should output: true

// Change the value of the variable 'name' to your full name.
name = "John Doe";
console.log(name);  // Should output: John Doe

// Change the value of the variable 'age' to your age plus 5.
age = age + 5;
console.log(age);  // Should output: 35

// Change the value of the variable 'age' to 50.
age = 50;
console.log(age);  // Should output: 50

// Change the value of the variable 'isLogged' to false.
isLogged = false;
console.log(isLogged);  // Should output: false
